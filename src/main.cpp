#include <iostream>
#include <vector>
#include <string>

int main()
{
    // std::vector<std::string> msg {"Hellcannot open source file "gtkmm.h"o", "C++", "World", "from", "VS Code", "and the C++ extension!"};
    // for (const std::string& word : msg)
    // {
    //     std::cout << word << " ";
    // }
    // std::cout << std::endl;
    std::cout << "getting started...\n";
    system("<chrome> http://google.com");
    return 0;
}